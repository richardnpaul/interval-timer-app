import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

/// Placeholder — EditGroupScreen will be replaced by the Routine Builder in Phase 4.
class EditGroupScreen extends ConsumerWidget {
  const EditGroupScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold(
      appBar: AppBar(title: const Text('Edit Group')),
      body: const Center(child: Text('Routine Builder coming in Phase 4.')),
    );
  }
}
