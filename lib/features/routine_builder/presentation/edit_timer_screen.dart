import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

/// Placeholder — EditTimerScreen will be replaced by the Routine Builder in Phase 4.
class EditTimerScreen extends ConsumerWidget {
  const EditTimerScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold(
      appBar: AppBar(title: const Text('New Timer')),
      body: const Center(child: Text('Routine Builder coming in Phase 4.')),
    );
  }
}
